/*let diffSel = Vue.createApp({
  data() {
    return {
      items: []
    }
  }
}).mount('#array-rendering');

Vue.createApp({
  data() {
    return {
      name: 'Vue.js'
    }
  },
  methods: {
    greet(event) {
      // `this` inside methods points to the Vue instance
      alert('Hello ' + this.name + '!')
      // `event` is the native DOM event
      if (event) {
        alert(event.target.tagName)
      }
    }
  }
}).mount('#e-createSequence')*/